---
- name: Create EC2 using exact_count
  hosts: localhost
  connection: local
  gather_facts: false

  vars:
    aws_region: us-east-2
    instance_type: t3.micro
    key_name: ray-vault-key
    sg_name: ansible-ec2-demo-sg
    instance_name: ansible-exact-demo

    nginx_user_data: |
      #!/bin/bash
      apt-get update
      apt-get install -y nginx
      systemctl enable nginx
      systemctl start nginx
      echo "Created by Ansible exact_count test" > /var/www/html/index.html

  tasks:
    - name: Find default VPC
      amazon.aws.ec2_vpc_net_info:
        region: "{{ aws_region }}"
        filters:
          is-default: "true"
      register: vpc_info

    - name: Set default VPC ID
      ansible.builtin.set_fact:
        default_vpc_id: "{{ vpc_info.vpcs[0].vpc_id }}"

    - name: Find subnet in default VPC
      amazon.aws.ec2_vpc_subnet_info:
        region: "{{ aws_region }}"
        filters:
          vpc-id: "{{ default_vpc_id }}"
      register: subnet_info

    - name: Set subnet ID
      ansible.builtin.set_fact:
        default_subnet_id: "{{ subnet_info.subnets[0].subnet_id }}"

    - name: Find latest Ubuntu 24.04 AMI
      amazon.aws.ec2_ami_info:
        region: "{{ aws_region }}"
        owners:
          - "099720109477"
        filters:
          name: "ubuntu/images/hvm-ssd-gp3/ubuntu-noble-24.04-amd64-server-*"
          architecture: x86_64
          virtualization-type: hvm
          root-device-type: ebs
      register: ubuntu_ami_info

    - name: Set latest Ubuntu AMI ID
      ansible.builtin.set_fact:
        ubuntu_ami_id: "{{ (ubuntu_ami_info.images | sort(attribute='creation_date') | last).image_id }}"

    - name: Create security group
      amazon.aws.ec2_security_group:
        name: "{{ sg_name }}"
        description: Ansible EC2 demo security group
        region: "{{ aws_region }}"
        vpc_id: "{{ default_vpc_id }}"
        rules:
          - proto: tcp
            from_port: 22
            to_port: 22
            cidr_ip: 0.0.0.0/0
            rule_desc: Allow SSH
          - proto: tcp
            from_port: 80
            to_port: 80
            cidr_ip: 0.0.0.0/0
            rule_desc: Allow HTTP
        rules_egress:
          - proto: all
            cidr_ip: 0.0.0.0/0
      register: web_sg

    - name: Ensure exactly one matching EC2 is running
      amazon.aws.ec2_instance:
        region: "{{ aws_region }}"
        image_id: "{{ ubuntu_ami_id }}"
        instance_type: "{{ instance_type }}"
        key_name: "{{ key_name }}"
        exact_count: 1
        wait: true
        user_data: "{{ nginx_user_data }}"
        filters:
          "tag:Name": "{{ instance_name }}"
          "tag:ManagedBy": Ansible
          "tag:Lab": exact-count-test
          instance-state-name: running
        vpc_subnet_id: "{{ default_subnet_id }}"
        security_groups:
          - "{{ web_sg.group_id }}"
        network:
          assign_public_ip: true
        tags:
          Name: "{{ instance_name }}"
          ManagedBy: Ansible
          Lab: exact-count-test
        state: running
      register: ec2_result

    - name: Show EC2 result
      ansible.builtin.debug:
        msg:
          instance_ids: "{{ ec2_result.instance_ids }}"
          public_ips: "{{ ec2_result.instances | map(attribute='public_ip_address') | list }}"
          terminated_ids: "{{ ec2_result.terminated_ids | default([]) }}"